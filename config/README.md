# Config

- **`providers-matrix.yaml`** – Provider options for **files**, **inference**, and **vector_io**, aligned with [llama-stack-distribution/distribution](https://github.com/opendatahub-io/llama-stack-distribution/tree/main/distribution). Used for provider-matrix test runs; see `docs/providers-matrix.md` and `local/scripts/run-tests-with-providers.sh`.
