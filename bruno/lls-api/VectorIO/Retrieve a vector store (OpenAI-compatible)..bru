meta {
  name: Retrieve a vector store (OpenAI-compatible).
  type: http
  seq: 5
  tags: [
    VectorIO
  ]
}

get {
  url: {{baseUrl}}/v1/vector_stores/:vector_store_id
  body: none
  auth: inherit
}

params:path {
  vector_store_id: 
}
