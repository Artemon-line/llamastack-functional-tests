meta {
  name: Unregister a dataset by its ID.
  type: http
  seq: 4
  tags: [
    Datasets
  ]
}

delete {
  url: {{baseUrl}}/v1beta/datasets/:dataset_id
  body: none
  auth: inherit
}

params:path {
  dataset_id: 
}
