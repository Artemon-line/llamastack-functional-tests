meta {
  name: Delete File
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/v1/files/{{file_id}}
  body: none
  auth: none
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.eql(200);
  });
}
