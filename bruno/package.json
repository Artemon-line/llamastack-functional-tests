{
  "devDependencies": {
    "@usebruno/cli": "^3.1.3"
  },
  "scripts": {
    "bruno:generate": "bru import openapi --source bruno/openapi.json --output bruno/ogx-api --collection-name \"OGX API\"",
    "bruno:run": "cd ogx-api && bru run . -r"
  }
}
