meta {
  name: Get a dataset by its ID.
  type: http
  seq: 3
  tags: [
    Datasets
  ]
}

get {
  url: {{baseUrl}}/v1beta/datasets/:dataset_id
  body: none
  auth: inherit
}

params:path {
  dataset_id:
}
