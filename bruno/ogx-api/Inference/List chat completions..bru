meta {
  name: List chat completions.
  type: http
  seq: 2
  tags: [
    Inference
  ]
}

get {
  url: {{baseUrl}}/v1/chat/completions
  body: none
  auth: inherit
}

params:query {
  limit: 20
  order: desc
  ~after:
  ~model:
}
