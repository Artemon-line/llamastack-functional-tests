meta {
  name: Retrieve a vector store file batch (OpenAI-compatible).
  type: http
  seq: 16
  tags: [
    VectorIO
  ]
}

get {
  url: {{baseUrl}}/v1/vector_stores/:vector_store_id/file_batches/:batch_id
  body: none
  auth: inherit
}

params:path {
  vector_store_id:
  batch_id:
}
