meta {
  name: Delete a vector store (OpenAI-compatible).
  type: http
  seq: 7
  tags: [
    VectorIO
  ]
}

delete {
  url: {{baseUrl}}/v1/vector_stores/:vector_store_id
  body: none
  auth: inherit
}

params:path {
  vector_store_id:
}
